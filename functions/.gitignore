node_modules/
*.local

# Firebase service account keys - NEVER commit
*.json
!package.json
!package-lock.json

# Firebase Functions environment variables (contain Twilio + other secrets)
*.env
.env
.env.*